var tree;
var inp;
var btn;
function setup(){
createCanvas(window.outerWidth,window.outerHeight-200);

background(0)

// translate(300,30);
tree = new Tree();

// tree.addData(4);
//  tree.addData(8);
//  tree.addData(2);
//  tree.addData(3);

 }
function draw(){
   
    // tree.show()
}
